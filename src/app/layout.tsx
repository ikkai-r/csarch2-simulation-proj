import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "IEEE-754 Binary-64 floating point converter",
  description: "An IEEE-754 Binary-64 floating point converter web application that accepts decimal and binary and converts to hexadecimal and binary equivalent",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
